# Demo Mode (데모 모드로 실행하려면 True로 설정)
# True로 설정하면 실제 SharePoint 없이 샘플 데이터로 테스트 가능
DEMO_MODE=True

# Microsoft Graph API Configuration
# Azure AD 앱 등록에서 얻은 값들을 입력하세요
# https://portal.azure.com/ -> Azure Active Directory -> App registrations
# DEMO_MODE=True일 경우 아래 값들은 무시됩니다
TENANT_ID=your-tenant-id-here
CLIENT_ID=your-client-id-here
CLIENT_SECRET=your-client-secret-here

# SharePoint Site ID (optional, can be obtained via Graph API)
# Example: contoso.sharepoint.com,12345678-1234-1234-1234-123456789012,12345678-1234-1234-1234-123456789012
SHAREPOINT_SITE_ID=

# Qdrant Vector Database Configuration
# 모드 선택: "local" (Docker 불필요) 또는 "server" (Docker 필요)
QDRANT_MODE=local

# Local 모드 설정 (QDRANT_MODE=local일 때 사용)
QDRANT_PATH=./qdrant_data

# Server 모드 설정 (QDRANT_MODE=server일 때 사용)
QDRANT_HOST=localhost
QDRANT_PORT=6333

# 컬렉션 이름
QDRANT_COLLECTION_NAME=spo_docs

# LLM Configuration (PwC GenAI Shared Service)
# LLM 모델 이름 (환경변수에서 읽음)
LLM_MODEL=gpt-4o

# API Keys
OPENAI_API_KEY=

# PwC GenAI Shared Service Base URL
PWC_GENAI_BASE_URL=https://genai-sharedservice-americas.pwcinternal.com/v1

# Embedding Configuration
EMBED_PROVIDER=openai
EMBEDDING_MODEL=azure.text-embedding-3-large
EMBEDDING_DIMENSION=1536

# Text Chunking Configuration
CHUNK_SIZE=1000
CHUNK_OVERLAP=200

# Optional: OpenAI API Key (LLM 및 임베딩 사용 시)
# OPENAI_API_KEY=sk-...

# Optional: Anthropic API Key (Claude 사용 시)
# ANTHROPIC_API_KEY=sk-ant-...

